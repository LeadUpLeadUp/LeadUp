const screens=[...document.querySelectorAll('.screen')];const toast=document.getElementById('toast');const state={userName:'',dog:{sex:'male'}};function showToast(m){toast.textContent=m;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)}function showScreen(id){screens.forEach(s=>s.classList.toggle('active',s.id===id));window.scrollTo({top:0,behavior:'smooth'})}document.querySelectorAll('[data-next]').forEach(b=>b.addEventListener('click',()=>showScreen(b.dataset.next)));document.querySelectorAll('[data-back]').forEach(b=>b.addEventListener('click',()=>showScreen(b.dataset.back)));document.getElementById('goRegister').addEventListener('click',()=>showScreen('screenRegister'));document.getElementById('togglePassword').addEventListener('click',()=>{const i=document.getElementById('loginPassword');i.type=i.type==='password'?'text':'password'});document.getElementById('loginForm').addEventListener('submit',e=>{e.preventDefault();state.userName=document.getElementById('loginEmail').value.split('@')[0]||'משתמש';showToast('התחברת בהצלחה');showScreen('screenWelcome')});document.getElementById('registerForm').addEventListener('submit',e=>{e.preventDefault();state.userName=document.getElementById('regName').value||'משתמש';showToast('ההרשמה הושלמה');showScreen('screenWelcome')});document.getElementById('googleLogin').addEventListener('click',()=>{state.userName='משתמש Google';showToast('התחברות Google תחובר בשלב הבא');showScreen('screenWelcome')});document.getElementById('googleRegister').addEventListener('click',()=>{state.userName='משתמש Google';showToast('הרשמת Google תחובר בשלב הבא');showScreen('screenWelcome')});document.querySelectorAll('.choice').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.choice').forEach(x=>x.classList.remove('active'));b.classList.add('active');state.dog.sex=b.dataset.sex}));document.getElementById('dogForm').addEventListener('submit',e=>{e.preventDefault();state.dog.name=document.getElementById('dogName').value||'הכלב שלך';state.dog.breed=document.getElementById('dogBreed').value;state.dog.age=document.getElementById('dogAge').value;state.dog.weight=document.getElementById('dogWeight').value;state.dog.notes=document.getElementById('dogNotes').value;showToast('פרופיל הכלב נשמר');showScreen('screenLocation')});document.getElementById('allowLocation').addEventListener('click',()=>{if(!navigator.geolocation){showToast('המכשיר לא תומך במיקום');showScreen('screenInterests');return}navigator.geolocation.getCurrentPosition(()=>{showToast('המיקום נשמר בהצלחה');showScreen('screenInterests')},()=>{showToast('לא אושר מיקום, אפשר להמשיך');showScreen('screenInterests')},{enableHighAccuracy:true,timeout:7000})});document.querySelectorAll('.service').forEach(b=>b.addEventListener('click',()=>b.classList.toggle('selected')));document.querySelector('[data-next="screenHome"]').addEventListener('click',()=>{document.getElementById('homeUserName').textContent=state.userName||'ברוך הבא';if(state.dog.name)document.getElementById('dogReminderTitle').textContent=`${state.dog.name} מוכן לתור הטיפוח הבא`});
